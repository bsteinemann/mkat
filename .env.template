# Required
DOMAIN=example.com
ACME_EMAIL=you@example.com
MKAT_USERNAME=admin
MKAT_PASSWORD=changeme

# Optional - Traefik dashboard (accessible at traefik.$DOMAIN)
# Generate credentials (replace YOUR_PASSWORD):
#   docker run --rm httpd:alpine htpasswd -nbB admin YOUR_PASSWORD | sed -e 's/\$/\$\$/g'
# Paste the output below (e.g. admin:$$2y$$05$$...)
# The $$ escaping is required for docker-compose to treat $ as literal.
TRAEFIK_DASHBOARD_AUTH=

# Optional - Telegram notifications
MKAT_TELEGRAM_BOT_TOKEN=
MKAT_TELEGRAM_CHAT_ID=

# Optional - Watchtower
WATCHTOWER_POLL_INTERVAL=3600
WATCHTOWER_NOTIFICATIONS=
WATCHTOWER_NOTIFICATION_URL=

# Optional - Logging
MKAT_LOG_LEVEL=Information
